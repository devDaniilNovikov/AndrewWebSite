# fix-db-identity-generation-contract handoff

Signature: HND fix-db-identity-generation-contract [ready] topics: backend, tracker → predecessor: 2026-07-24-163204-fix-db-identity-generation-contract-handoff.md

## Durable — safe to cite later

- The user explicitly authorized the Ready transition and squash merge for
  PR #37. Production deployment remains outside that authorization.
- The change only strengthens the PostgreSQL catalog contract test to require
  `GENERATED BY DEFAULT` identity columns. The Flyway migration, runtime code,
  HTTP API, CI workflows, and frontend are unchanged.
- TDD RED/GREEN, the full 77-test Maven gate with PostgreSQL 18.4, the JaCoCo
  gate, scope and secret checks, and an independent review all passed.

## Snapshot at 2026-07-24T16:42Z — re-verify live before use

- Active incidents and ongoing investigations: none.
- Draft [PR #37](https://github.com/devDaniilNovikov/AndrewWebSite/pull/37)
  is mergeable and clean against `main`. It points to
  `59204aee423426b55138fb1ab908b68ac9810718` before this readiness
  checkpoint.
- On that exact head, Repository policy, verify, dependency-security,
  Java security, CodeQL, Semgrep, and Snyk succeeded. Expected event-specific
  companion jobs were skipped.
- Known issue to watch: this metadata checkpoint creates a new exact PR head,
  so no previous check result may be reused for the merge decision.
- No production service, secret, user-owned desktop-root file, or frontend
  path was changed.

## Next steps — conditional, each requires the stated live check

1. Commit and normally push this readiness checkpoint, then wait for every
   required and applicable check on its exact SHA.
2. Confirm PR #37 still targets `main`, mark it Ready, and re-check the exact
   head, mergeability, review state, and any newly triggered checks.
3. Squash-merge PR #37 with an exact-head guard, then verify the merge on
   `origin/main`, post-merge checks, and the retained source branch.
4. Do not deploy production. The next backend task reconciles row 32 to
   `merged` and moves this handoff chain to closed chains.
