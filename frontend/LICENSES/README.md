# Third-party font notice

The frontend bundles Inter Variable v4.1 locally through the exact
`inter-ui@4.1.1` package. The built static export copies the font files into
its own `_next/static` assets and makes no remote font request.

Inter is distributed under the SIL Open Font License 1.1. The complete
license and attribution are recorded in `Inter-OFL-1.1.txt`.
