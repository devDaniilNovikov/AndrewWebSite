spring:
  config:
    activate:
      on-profile: local
app:
  leads:
    fingerprint-key: ${LEAD_FINGERPRINT_HMAC_KEY}
  telegram:
    bot-token: ${TELEGRAM_BOT_TOKEN}
    chat-id: ${TELEGRAM_CHAT_ID}
    base-url: ${TELEGRAM_BASE_URL}
  web:
    local-cors-origins: ${LOCAL_CORS_ORIGINS:}
